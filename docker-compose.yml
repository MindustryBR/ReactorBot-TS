version: "3.9"

services:
  reactorbot:
    container_name: reactorbot
    build: .
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data